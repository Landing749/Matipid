{
  "name": "matipid-export-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that generates PDF/Excel exports of MATIPID Finance and Audit records, reading directly from the Firebase RTDB REST API.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^1.3.0",
    "@cloudflare/workers-types": "^5.20260830.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "xlsx": "^0.18.5"
  }
}
